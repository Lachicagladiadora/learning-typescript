// MODULES -> allows you to split the code to organize them better and work on them independently
function subtract(firstNumber: number, secondNumber: number): void {
  // return 
  console.log(firstNumber - secondNumber)
}
export {subtract}